@{
    Layout = "_Layout";
    //_viewStart used to reolace any c# code in the Views before starting the code of the view
    //it is shated between all views by default
    //it will set the property in the view called layout by _layout
}
